# Project Scripts

Operational scripts are grouped by purpose:

- `admin/`: local admin-user setup utilities
- `data/`: estate-data restoration utilities
- `debug/`: focused debugging helpers
- `integration/`: integration and protocol checks
- `simulation/`: sensor and device simulators

Existing recurring checks remain directly under `scripts/` for compatibility.
Run commands from the repository root so imports resolve consistently.
